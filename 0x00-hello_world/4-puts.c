#include<stdio.h>
int main(void)
	/**
	 * main - must return void agurment and not any other
	 * void - void value
	 * return - always 0 (succes)
	 * 0 - the number to be returned
	 */
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
