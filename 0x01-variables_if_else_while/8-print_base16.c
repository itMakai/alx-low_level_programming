/**
 * main - Entry point
 * Return: Always 0 (success)
 */
#include<stdio.h>
int main(void)
{
	int n;
	int m;

	while (n = 48 && n <= 57)
	{
		putchar(n);
		n++;
	}
	while (m = 97 && m <= 102)
	{
		putchar(m);
		m++;
	}
	putchar('\n');
	return (0);
}
